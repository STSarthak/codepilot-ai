apiVersion: apps/v1
kind: Deployment
metadata:
  name: codepilot-proxy
  namespace: codepilot-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codepilot-proxy
  template:
    metadata:
      labels:
        app: codepilot-proxy
    spec:
      containers:
        - name: proxy
          image: codepilot-proxy:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: codepilot-proxy-svc
  namespace: codepilot-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: codepilot-proxy
